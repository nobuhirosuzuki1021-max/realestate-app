# CLAUDE.md — realestate-app

## プロジェクト概要

不動産情報を扱うWebアプリケーション。

## 技術スタック

(プロジェクト開始時に更新すること)

## ディレクトリ構成

(プロジェクト開始時に更新すること)

---

## Git 運用ルール

### 基本方針

**コードを変更するたびに、必ず GitHub にプッシュすること。**
変更を積み上げてからまとめてプッシュするのは禁止。作業の安全性と履歴の透明性を保つため、変更単位でコミット＆プッシュを行う。

### コミット手順

1. 変更内容を確認する
   ```
   git status
   git diff
   ```
2. 関連ファイルをステージングする（`git add -A` は使わず、ファイルを明示する）
   ```
   git add <変更ファイル>
   ```
3. 変更内容を端的に表すメッセージでコミットする
   ```
   git commit -m "変更内容の要約"
   ```
4. **即座に** GitHub へプッシュする
   ```
   git push origin <ブランチ名>
   ```

### コミットメッセージ規約

| プレフィックス | 用途 |
|---|---|
| `feat:` | 新機能の追加 |
| `fix:` | バグ修正 |
| `refactor:` | 動作を変えないコード整理 |
| `docs:` | ドキュメントのみの変更 |
| `test:` | テストの追加・修正 |
| `chore:` | ビルド設定・依存関係の更新など |

例: `feat: 物件一覧ページに価格フィルターを追加`

### ブランチ戦略

- `main` — 本番相当の安定ブランチ。直接コミットは禁止
- `develop` — 統合ブランチ。日常的な作業はここかフィーチャーブランチから
- `feature/<名前>` — 機能ごとの作業ブランチ

### 禁止事項

- `.env` ファイルや API キーなどの機密情報をコミットしない
- `--no-verify` でフックをスキップしない
- `main` への force-push は禁止
- 機密ファイルは `.gitignore` に必ず追加する

---

## コーディング規約

- コメントは「なぜ」を書く。「何をしているか」は書かない
- 不要な抽象化・将来の拡張のための準備コードは書かない
- セキュリティ: ユーザー入力・外部 API レスポンスは必ず検証する
